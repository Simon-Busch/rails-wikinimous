<h1>Here is the list of all your articles bro 🧐</h1>

<ul>
  <% @articles.each do |article|%>
    <li> <%= link_to  "Delete ⌲", destroy_article_path(article.id), method: :delete, data: {confirm: 'Do you want to delete this post ?'}%>
    <%= link_to article.title, article_path(article) %>
    </li>
  <%end%>
</ul>

<%= link_to  "Create an article", articles_new_path %>